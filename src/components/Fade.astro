---
const { origin } = Astro.props;
---

<div
    x-data
    class={`${origin} flex flex-col w-full h-full opacity-0 scale-[0.95] ease-in-out duration-[750ms] transition-all`}
    x-intersect:enter="$el.classList.add('opacity-100', 'scale-[1]')"
    x-intersect:leave="$el.classList.remove('opacity-100', 'scale-[1]')"
>
    <slot />
</div>
